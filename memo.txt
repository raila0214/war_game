//package.json(設計中のため一時的にvite buildのみにしている)
    "build": "tsc -b && vite build",

やりたい
・GameScreenがごちゃついてるから設定フェーズを別UIに分裂するか
・Boardが仮の簡素な物だから余裕あったら整える。
・ゲームバランスの調整（HP）
・部隊ごとの速さの実装（現在は全部隊おんなじペースで動いてる）
・複数部隊が同じ座標に存在できるようになってしまっているので、都度、進行方向に別部隊がいた場合は目的地までのルートを探索し直すとか